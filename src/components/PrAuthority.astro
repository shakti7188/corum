---
// PR Authority — dark editorial band that stages the PR proof points
// just below the Community / Influencer section. Headline is the brand
// thesis ("Coverage that moves markets."), the proof is two big numbers
// (8,000+ coverage pieces, 40,000+ media touches) and a Tier-1 logo
// strip. No grid, no marquee — quiet, magazine-feel layout.

const tierOneLogos = [
  { src: '/media-logos/Forbes.webp',         alt: 'Forbes' },
  { src: '/media-logos/Bloomberg.webp',      alt: 'Bloomberg' },
  { src: '/media-logos/cointelegraph.png',   alt: 'CoinTelegraph' },
  { src: '/media-logos/nasdaq.webp',         alt: 'Nasdaq' },
  { src: '/media-logos/Coindesk.webp',       alt: 'CoinDesk' },
  { src: '/media-logos/cnbc.png',            alt: 'CNBC' },
  { src: '/media-logos/yahoo-finance.png',   alt: 'Yahoo Finance' },
  { src: '/media-logos/reuters.png',         alt: 'Reuters' },
];
---

<section class="c8-pr">
  <div class="c8-pr__inner">
    <header class="c8-pr__head" data-reveal-stagger="100">
      <p class="c8-pr__eyebrow" data-reveal="up">— Press authority</p>
      <h2 class="c8-pr__heading" data-reveal="up">
        Coverage that <span class="c8-pr__heading-accent">moves markets.</span>
      </h2>
      <p class="c8-pr__sub" data-reveal="up">
        We've placed clients in every Tier-1 outlet that matters — across token launches,
        category-defining product reveals, and ongoing narrative work. The relationships
        are real, the placements are earned, and the coverage compounds.
      </p>
    </header>

    <div class="c8-pr__stats" data-reveal-stagger="120">
      <div class="c8-pr__stat" data-reveal="up">
        <span class="c8-pr__stat-num">8,000+</span>
        <span class="c8-pr__stat-lbl">Coverage pieces secured</span>
      </div>
      <div class="c8-pr__stat" data-reveal="up">
        <span class="c8-pr__stat-num">40,000+</span>
        <span class="c8-pr__stat-lbl">Media touches across campaigns</span>
      </div>
      <div class="c8-pr__stat" data-reveal="up">
        <span class="c8-pr__stat-num">10,000+</span>
        <span class="c8-pr__stat-lbl">Outlets in active relationship</span>
      </div>
    </div>

    <div class="c8-pr__logos" data-reveal="up">
      {tierOneLogos.map((m) => (
        <img src={m.src} alt={m.alt} loading="lazy" class="c8-pr__logo" />
      ))}
    </div>
  </div>
</section>

<style is:global>
  .c8-pr {
    position: relative;
    background: #0a0a0a;
    color: #fff;
    padding: clamp(4.5rem, 9vh, 7.5rem) clamp(1.5rem, 5vw, 5rem);
    overflow: hidden;
    isolation: isolate;
  }
  .c8-pr::before {
    content: '';
    position: absolute;
    inset: 0;
    background:
      radial-gradient(900px circle at 80% 20%, rgba(242,191,34,0.06) 0%, transparent 60%),
      radial-gradient(800px circle at 10% 90%, rgba(242,191,34,0.04) 0%, transparent 60%);
    pointer-events: none;
    z-index: 0;
  }
  .c8-pr__inner {
    position: relative;
    z-index: 1;
    max-width: 1200px;
    margin: 0 auto;
  }
  .c8-pr__head {
    max-width: 56rem;
    margin: 0 0 clamp(2.5rem, 5vh, 3.75rem);
  }
  .c8-pr__eyebrow {
    margin: 0 0 1rem;
    font-family: var(--montreal);
    font-size: 0.75rem;
    font-weight: 600;
    letter-spacing: 0.22em;
    text-transform: uppercase;
    color: var(--c8-gold);
  }
  .c8-pr__heading {
    margin: 0 0 1.25rem;
    font-family: var(--font-display);
    font-size: clamp(2rem, 4.4vw, 3.4rem);
    font-weight: 400;
    line-height: 1.05;
    letter-spacing: -0.03em;
    color: #fff;
  }
  .c8-pr__heading-accent {
    color: var(--c8-gold-dark);
    font-weight: 400;
  }
  .c8-pr__sub {
    margin: 0;
    font-family: var(--montreal);
    font-size: clamp(1rem, 1.2vw, 1.15rem);
    line-height: 1.6;
    color: rgba(255,255,255,0.72);
    max-width: 48rem;
  }

  /* ─── Stats row ─── */
  .c8-pr__stats {
    display: grid;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    gap: clamp(1.25rem, 2.4vw, 2rem);
    margin: 0 0 clamp(2.5rem, 5vh, 3.75rem);
  }
  .c8-pr__stat {
    display: flex;
    flex-direction: column;
    gap: 0.4rem;
    padding: 1.5rem 1.25rem;
    background: rgba(255,255,255,0.04);
    border: 1px solid rgba(255,255,255,0.08);
    border-radius: 14px;
    transition: border-color 0.4s ease, background 0.4s ease;
  }
  .c8-pr__stat:hover {
    border-color: rgba(242,191,34,0.4);
    background: rgba(255,255,255,0.06);
  }
  .c8-pr__stat-num {
    font-family: var(--font-display);
    font-size: clamp(1.65rem, 3.2vw, 2.4rem);
    font-weight: 400;
    letter-spacing: -0.02em;
    color: var(--c8-gold-dark);
    line-height: 1;
  }
  .c8-pr__stat-lbl {
    font-family: var(--montreal);
    font-size: 0.92rem;
    color: rgba(255,255,255,0.68);
    line-height: 1.35;
  }

  /* ─── Tier-1 logo strip ─── */
  .c8-pr__logos {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    justify-content: center;
    gap: clamp(1.5rem, 3vw, 2.5rem);
    padding: clamp(1.75rem, 3vh, 2.5rem) 0 0;
    border-top: 1px solid rgba(255,255,255,0.08);
  }
  .c8-pr__logo {
    height: 26px;
    width: auto;
    max-width: 140px;
    object-fit: contain;
    filter: brightness(0) invert(1);
    opacity: 0.55;
    transition: opacity 0.3s ease, transform 0.3s ease;
  }
  .c8-pr__logo:hover {
    opacity: 1;
    transform: translateY(-2px);
  }

  @media (max-width: 768px) {
    .c8-pr__stats { grid-template-columns: 1fr; }
    .c8-pr__logo { height: 22px; }
  }
</style>
